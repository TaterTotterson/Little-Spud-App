<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Pairing QR codes may point at an HTTP-only Tater on the local network. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
